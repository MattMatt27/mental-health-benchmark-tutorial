# =============================================================================
# API Keys for Running Your Own Benchmark Experiment
# =============================================================================
#
# You only need these if you want to run the benchmark yourself (Notebook 02).
# Notebook 01 ("Explore Our Results") works without any API keys.
#
# Copy this file to .env and fill in the keys you need. You don't need all
# three — just the providers whose models you want to test.
#
# COST NOTE: Running the full experiment as configured in the paper
# (~25,920 API calls across 9 models) costs roughly $50–100 depending on
# model mix. A small-scale test with one model and one condition (10 calls)
# costs a few cents.
# =============================================================================

# ── OpenAI ──────────────────────────────────────────────────────────────────
# Models: GPT-4o, GPT-3.5 Turbo, etc.
#
# 1. Create an account at https://platform.openai.com/
# 2. Go to https://platform.openai.com/api-keys
# 3. Click "Create new secret key"
# 4. Copy the key (it starts with "sk-")
#
OPENAI_API_KEY=your-openai-key-here

# ── Anthropic ───────────────────────────────────────────────────────────────
# Models: Claude Opus 4, Claude Sonnet 4, Claude 3.5 Sonnet, Claude 3.5 Haiku, etc.
#
# 1. Create an account at https://console.anthropic.com/
# 2. Go to https://console.anthropic.com/settings/keys
# 3. Click "Create Key"
# 4. Copy the key (it starts with "sk-ant-")
#
ANTHROPIC_API_KEY=your-anthropic-key-here

# ── Google (Gemini) ─────────────────────────────────────────────────────────
# Models: Gemini 2.5 Pro, Gemini 2.5 Flash, Gemini 2.0 Flash, etc.
#
# 1. Go to https://aistudio.google.com/apikey
# 2. Click "Create API key"
# 3. Select or create a Google Cloud project when prompted
# 4. Copy the key
#
GOOGLE_API_KEY=your-google-key-here
